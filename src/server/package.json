{
  "name": "chat-bubble-server",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "start": "node --env-file=.env index.js",
    "check": "node --check index.js && node --check adapter.js && node --check sse_hub.js"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.14.4",
    "@grpc/proto-loader": "^0.8.1",
    "google-proto-files": "^5.0.2"
  }
}
